const ButtonType = {
  primary: "primary",
  textOnly: "textOnly",
};

const ButtonSize = {
  small: "small",
  medium: "medium",
  large: "large",
};

Object.freeze(ButtonType, ButtonSize);

export default ButtonType;
